when:
  - event: ["push", "manual"]
    branch: ["main"]
  - event: ["pull_request"]
    branch: ["main"]

engine: "nixery"

dependencies:
  nixpkgs:
    - statix
    - deadnix

steps:
  - name: "statix check"
    command: "statix check ."
  - name: "deadnix"
    command: "deadnix --fail ."
